#include<stdio.h>
float area_circle(float);
int main()
{
    float r,area;
    printf("enter the radius: ");
    scanf("%f",&r);
   area=area_circle(r);
    printf("%f",area);
return 0;
}
float area_circle(float r)
{
 float area;
 area = 3.14*(r*r);
return area;
}
